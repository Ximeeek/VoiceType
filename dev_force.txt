# VoiceType Developer Overrides / Opcje Wymuszania dla Deweloperów
#
# Plik konfiguracyjny pozwalający deweloperom na wymuszanie zachowań aplikacji w trybie debug.
# Ten plik jest całkowicie ignorowany w wersjach produkcyjnych (release).
#
# Użycie / Usage:
# Klucz=Wartość (np. first_start=1)
# 1 = Włączone / Wymuszone (Enabled / Forced)
# 0 = Wyłączone / Stłumione (Disabled / Suppressed)
# Zakomentowane linią z '#' = Zachowanie domyślne (Default behavior)

# 1. first_start
# Wymusza uruchomienie kreatora konfiguracji początkowej (onboarding).
# 1 = Wymuś otwarcie kreatora (Force open)
# 0 = Zablokuj otwarcie kreatora (Block open)
# Domyślnie: Wczytywane z konfiguracji użytkownika (config.toml).
first_start=1

# 2. changelog
# Wymusza pokazanie okna "Co Nowego" z historią zmian pobraną z GitHub.
# 1 = Wymuś otwarcie okna ze zmianami (Force open changelog)
# 0 = Zablokuj otwarcie okna ze zmianami (Block open changelog)
# Domyślnie: Pokazywane tylko raz przy pierwszym uruchomieniu po aktualizacji wersji.
changelog=1
